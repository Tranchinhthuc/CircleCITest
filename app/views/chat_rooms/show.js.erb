$("#messages-area").html("<%= escape_javascript(render('messages/message', messages: @messages)) %>");
$("#chat-form").html("<%= escape_javascript(render(partial: 'messages/message_form', locals: {message: @message, chat_room: @chat_room})) %>");
$('.messages-box').scrollTop($('.messages-box')[0].scrollHeight);
